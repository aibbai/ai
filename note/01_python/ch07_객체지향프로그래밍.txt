7장 객체지향프로그래밍

<<<<<<< HEAD
- class : 속성(변수)와 메소드(함수)를 갖는 객체를 만들기 위한 설계도
		모든 클래스는 object를 상속받는다(__str__(self), ...)

- class 내에서는 
	생성자 (생성자가 없으면 디폴트 생성자 자동생성) 및 소멸자
	인스턴스변수(멤버변수) vs 클래스변수 vs static 변수 
	인스턴스 메소드(일반함수) vs 클래스메소드 vs static메소드

- class 클래스명(상속받을 클래스명):
	클래스변수
	__static변수

	def __init__(self, [매개변수, ...]):
		self.인스턴스변수 = 매개변수 초기화
	def __del__(self):
		객체 소멸시 수행할 명령어
	def 인스턴스메소드명(self, [매개변수...]):
		명령어
	@classmethod
	def 클래스메소드명(cls, [매개변수,...]):
		cls.클래스변수와 관련된 로직(클래스명.클래스메소드명() 호출)
	@staticmethod
	def static메소드명([매개변수,...]):
		코드

- 객체변수(인스턴스) = Person(초기화될 멤버변수 값들)
	ex. 객체변수.__dict__객체를 딕셔너리 형태로 보여줌

- 상속 : 부모클래스로부터 자식클래스에 모든 속성을 받는 것
	super() :  '부모클래스의'를 의미
=======
1절. 객체와 클래스
	- class 클래스명 (클래스명은 항상 대문자로 시작)
	- 클래스도 독스트링 가능

2절. 생성자와 소멸자
	- 생성자(__init__(self, ...) : 객체가 생성될 때 자동 실행
		  		    : 인스턴스 변수 초기화 포함 -> 생성자 함수 중복정의(오버로딩) 불가
		                    : 없으면 default 생성자 자동 생성 
						* default 생성자 :  def __innit__(self)

									pass
						
					


	- 소멸자(__del__()) : 객체가 소멸될 때 자동 실행 (참조한 변수가 없어질 때)
				 : 소멸시 필요한 코드 포함
				 : 인스턴스 객체의 레퍼런스 카운트가 0이 될 때 실행

3절. 변수와 메소드

	- 클래스 변수 : 클래스 내부에 선언 => 생성된 모든 인스턴스(객체)에 동일한 값을 가짐
		   : 클래스 변수를 수정하면 모든 인스턴스에 반영될 수 O
		   : 객체 변수 수정하면 해당 객체에만 반영


>>>>>>> 0b24b4d18da0274f52b478b4b42c5e380fdbc036
